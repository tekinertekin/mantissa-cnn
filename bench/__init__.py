"""Benchmark harness skeleton for mantissa-cnn (protocol fixed, code stubbed).

The protocol constants live in bench.protocol-style module attributes of
speed.py / accuracy.py; plots.py renders their JSON outputs. A later phase
fills in the implementations — the docstrings in each module are the spec.
"""
